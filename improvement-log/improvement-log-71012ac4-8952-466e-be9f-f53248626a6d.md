# Improvement Log — Session 71012ac4-8952-466e-be9f-f53248626a6d

## Tracker

- [ ] 2026-08-25 — WebFetch returned stubs for both Apple doc URLs; escalation path worked but cost two wasted round-trips

## Log

### 2026-08-25 — Apple doc URLs returned stubs on WebFetch

**What happened:** Fetched `support.apple.com/en-us/121115` and `developer.apple.com/documentation/foundationmodels/systemlanguagemodel` with WebFetch. Both returned "content truncated"/title-only stubs. Recovered via WebSearch, which had the answer directly.
**Why this was inefficient:** Global CLAUDE.md already predicts this ("Apple HIG and Apple API docs are JS-rendered"). For a *factual device-support question* (as opposed to reading an API surface), WebSearch is the faster first hop — the answer is widely reported and doesn't need the primary doc.
**What better looks like:** WebFetch-first applies to reading API/spec detail off Apple docs. For "which devices support X" / "when did Apple ship Y", go to WebSearch first, then confirm against the Apple page only if the search results disagree. Also: `developer.apple.com/tutorials/data/…json` is the machine-readable path for Apple API docs and is worth trying before the browser escalation.
