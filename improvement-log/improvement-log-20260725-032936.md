# Improvement Log — Session 20260725-032936

## Tracker

- [ ] 2026-07-25 — Codex delegation failed on stale auth token; blocked Step 1 mid-build
- [ ] 2026-07-25 — Codex default model (gpt-5.6-sol) unsupported by installed CLI; needed explicit --model override
- [ ] 2026-07-25 — Codex (spark) Step 2 misses caught in verify: actor-isolation on pure static helpers + missing test import
- [ ] 2026-07-25 — Codex (spark) Step 4 misses: platform #if gating (iOS-only helper used on macOS) + missing return in getter

## Log

### 2026-07-25 — Codex delegation failed on stale auth token

**What happened:** Delegated the NotoTags package build to Codex via `/codex:rescue --background`. The Codex job failed 13s in with "access token could not be refreshed because your refresh token was already used." Required Eugene to interactively re-auth (`codex login`).
**Why it matters:** Codex auth can silently go stale between sessions. A background delegation then fails fast but only surfaces after a poll. Costs a round-trip.
**What better looks like:** Before delegating a substantial task, a cheap auth pre-check (e.g. `codex-companion.mjs` auth/status probe or a trivial `codex exec` echo) could catch stale auth before authoring/queuing the real run — fail fast at ~0s instead of after the brief + launch.

### 2026-07-25 — Codex default model unsupported by installed CLI

**What happened:** After re-auth, the fresh delegation failed again: the local Codex default model `gpt-5.6-sol` requires a newer Codex CLI/app than installed (400 invalid_request_error). Fixed by passing `--model gpt-5.3-codex-spark` explicitly.
**Why it matters:** Two consecutive infra failures cost ~2 extra round-trips before any code ran. The Codex CLI is behind the server's default model.
**What better looks like:** (1) Keep the Codex CLI updated, OR (2) default all delegations in this environment to an explicit supported `--model` until the CLI is upgraded. Consider upgrading the Codex CLI so UI-heavy later steps can use a stronger model than spark.

### 2026-07-25 — Codex (spark) Step 2 misses caught in verification

**What happened:** Codex on gpt-5.3-codex-spark implemented TagController + Xcode wiring correctly overall, but (a) left pure static helpers main-actor-isolated so they couldn't be called from the detached background scan (compile error), and (b) omitted `import NotoTags` in the new test target file. Both caught by my flowdeck build + test verification and fixed inline in ~2 min.
**Why it matters:** Spark is fast but weaker on Swift concurrency isolation and cross-module test imports. Confirms the verify-everything discipline pays off; "Codex says done" ≠ done.
**What better looks like:** For delegations touching @MainActor types + background tasks, pre-emptively note in the brief: "mark pure static helpers `nonisolated`; test files must `import` the package under test." Cheap spec hardening that prevents predictable spark misses.
