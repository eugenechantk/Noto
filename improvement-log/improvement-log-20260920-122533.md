# Improvement Log — Session 20260920-122533

## Tracker

- [ ] 2026-09-20 — Safari share-flow automation: label taps flaky, coordinate taps hit page content while the toolbar was collapsed (navigated to another article twice); Return key via `key return` did not register

- [ ] 2026-09-20 — 208 tracked files under `.codex/evidence/` vanished from the working tree (dir mtime reverted to Aug 24) — Syncthing-style overwrite; restored from git

## Log

### 2026-09-20 — `.codex/evidence` reverted to an old copy on disk

**What happened:** At session start `git status` in the main checkout listed 208 tracked files under `.codex/evidence/` as deleted — every audit folder from 2026-04 through yesterday's 20260919-010500, files that were present at 11:50 on 2026-09-19 when I last committed. The directory's mtime reads Aug 24 18:35, i.e. the whole folder was replaced by an older snapshot. The link-preview worktree showed the identical deletion list. Nothing in this session touched those paths before the status ran.
**Why this matters:** Same failure family as the 2026-09-11 `flowdeck-guard.sh` overwrite in `~/.claude` — a sync tool restoring a stale copy over a live tree. Here git had everything, so `git checkout -- .codex/evidence` restored all 682 tracked files; an uncommitted evidence folder would have been lost for good.
**What better looks like:** Commit evidence folders in the same turn they are produced (already the habit); on session start, treat a burst of unexplained `D` entries as a sync revert and restore from git before anything else. Eugene should check whether the Noto repo (or `.codex/`) is in a Syncthing folder shared with the other Mac and exclude `.codex/evidence` or the whole repo from sync.

### 2026-09-20 — Safari share-flow automation cost three rounds

**What happened:** On the fresh iOS 26.5 sim, `tap "More"`/`tap "Share"` returned "Element not found" intermittently, and my fallback coordinate taps for the toolbar landed on page content because Safari's toolbar had collapsed after the page scrolled — twice navigating to a different article. Separately, `flowdeck ui simulator key return` returned no success, so the Capture-tab check ended with URL and text on one line (no card, by design).
**Why this matters:** Each blind tap costs a round-trip plus a screenshot; the earlier share-flow recipe assumed a freshly loaded page with the toolbar expanded.
**What better looks like:** Always tap the URL pill (200,843) first to expand Safari's toolbar, then coordinates; and check `flowdeck ui simulator key --help` for the Return key name before typing multi-line input (or type "\n"). Add both to `project_noto2_target` sim gotchas.

